<a href="<%= user_path(current_user) %>"></a>
<h1>
	<%= current_user.name %>
</h1>
<div>
	  <%= avatar_for(current_user, 0) %>
</div>
<span>
	<%= link_to "view my profile", current_user %>
</span>
<span>
	<%= pluralize(current_user.microposts.count, "micropost") %>
</span>