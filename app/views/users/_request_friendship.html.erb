<%= form_for(current_user.friendships.build(friend_id: @friend ? @friend.id : @user.id), remote: true) do |f| %>
	<div><%= f.hidden_field :friend_id %></div>
	<%= f.submit "Request Friendship", class: "btn btn-large btn-primary" %>
<% end %>